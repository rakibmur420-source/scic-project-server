# Server
PORT=5000
NODE_ENV=development

# Database (PostgreSQL - use Supabase, NeonDB, or local Postgres)
DATABASE_URL="postgresql://username:password@host:5432/dbname?schema=public"

# JWT
JWT_SECRET=your_super_secret_jwt_key_change_this
JWT_EXPIRES_IN=7d
